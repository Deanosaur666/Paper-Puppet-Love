Screens:

"Modes" or "Programs"
    Use polymorphism -_-
    Load, Update, Draw Methods for whatever the loaded "program" is...
        (No big IF/Switch statements...)
    Command line args can select the starting "program"
    


Sprite edit screen
    View all textures
    Select texture file, quad, anchor
    Save by name (a unique key)
    Create sprite set
    Assign sprite to sprite set

Skeleton edit screen
    Create skeleton and give it a name
    Select current SpriteSet for display
    Sub screens: part blueprint editor, animation/frame editor

Part Blueprint editor
    Parts blueprints are part of a skeleton (a skeleton must be created first)
    Set part default sprite index (display with this for convenience in editor)
    Set part default layer
    Set part parent, offset, IK, position lock
    Set part hit balls

Animation editor
    Create an animation with a name
    Create a frame for an animation
    Copy a frame
    Set a frame's duration
    Move a frame in the timeline maybe
    Rotate, move, scale parts (the meat of the program)
    Set IK lock (for feet and stuff)
    Reverse IK direction (swap between two possible states)
    Change part sprites
    Change part layers

    Copy animation
    Copy animation with just starting frame (attack from idle, for example)